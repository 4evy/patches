From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: 4evy <git@evy.pink>
Date: Thu, 1 Jan 1970 00:00:00 +0000
Subject: apprt: edit scrollback in a terminal surface

Add a `scrollback-editor` command for opening text exports inside Ghostty.
A dedicated application action lets macOS and GTK temporarily replace the
current pane, restore the original surface when the editor exits, and remove
the exported directory without overloading URL handling.

Scrollback exports now include the visible screen, while alternate screens
export their active contents. Unsupported runtimes retain the existing
operating-system opener fallback.

Refs #2504
Refs #3496

---
 include/ghostty.h                             |   9 ++
 .../Terminal/BaseTerminalController.swift     |   5 +-
 .../Terminal/TerminalController.swift         |   6 +-
 macos/Sources/Ghostty/Ghostty.App.swift       | 101 ++++++++++++
 .../Surface View/SurfaceView_AppKit.swift     |   6 +
 src/Surface.zig                               | 109 +++++++++++--
 src/apprt/action.zig                          |  32 ++++
 src/apprt/gtk/class/application.zig           | 149 ++++++++++++++++++
 src/apprt/gtk/class/split_tree.zig            |  21 +++
 src/config/Config.zig                         |  38 +++++
 10 files changed, 457 insertions(+), 19 deletions(-)

diff --git a/include/ghostty.h b/include/ghostty.h
index 518af3f..7517a12 100644
--- a/include/ghostty.h
+++ b/include/ghostty.h
@@ -878,6 +878,13 @@ typedef struct {
   uintptr_t len;
 } ghostty_action_open_url_s;
 
+// apprt.action.EditScrollback.C
+typedef struct {
+  const char* directory;
+  uintptr_t directory_len;
+  const char* command;
+} ghostty_action_edit_scrollback_s;
+
 // apprt.action.CloseTabMode
 typedef enum {
   GHOSTTY_ACTION_CLOSE_TAB_MODE_THIS,
@@ -1009,6 +1016,7 @@ typedef enum {
   GHOSTTY_ACTION_READONLY,
   GHOSTTY_ACTION_COPY_TITLE_TO_CLIPBOARD,
   GHOSTTY_ACTION_MOVE_TAB_TO_NEW_WINDOW,
+  GHOSTTY_ACTION_EDIT_SCROLLBACK,
 } ghostty_action_tag_e;
 
 typedef union {
@@ -1052,6 +1060,7 @@ typedef union {
   ghostty_action_search_selected_s search_selected;
   ghostty_action_readonly_e readonly;
   ghostty_action_open_config_e open_config;
+  ghostty_action_edit_scrollback_s edit_scrollback;
 } ghostty_action_u;
 
 typedef struct {
diff --git a/macos/Sources/Features/Terminal/BaseTerminalController.swift b/macos/Sources/Features/Terminal/BaseTerminalController.swift
index 79ca5b6..ebeeb8a 100644
--- a/macos/Sources/Features/Terminal/BaseTerminalController.swift
+++ b/macos/Sources/Features/Terminal/BaseTerminalController.swift
@@ -541,7 +541,8 @@ class BaseTerminalController: NSWindowController,
         _ newTree: SplitTree<Ghostty.SurfaceView>,
         moveFocusTo newView: Ghostty.SurfaceView? = nil,
         moveFocusFrom oldView: Ghostty.SurfaceView? = nil,
-        undoAction: String? = nil
+        undoAction: String? = nil,
+        undoable: Bool = true
     ) {
         // Setup our new split tree
         let oldTree = surfaceTree
@@ -553,7 +554,7 @@ class BaseTerminalController: NSWindowController,
         }
 
         // Setup our undo
-        guard let undoManager else { return }
+        guard undoable, let undoManager else { return }
         if let undoAction {
             undoManager.setActionName(undoAction)
         }
diff --git a/macos/Sources/Features/Terminal/TerminalController.swift b/macos/Sources/Features/Terminal/TerminalController.swift
index 7070a48..0731966 100644
--- a/macos/Sources/Features/Terminal/TerminalController.swift
+++ b/macos/Sources/Features/Terminal/TerminalController.swift
@@ -190,7 +190,8 @@ class TerminalController: BaseTerminalController, TabGroupCloseCoordinator.Contr
         _ newTree: SplitTree<Ghostty.SurfaceView>,
         moveFocusTo newView: Ghostty.SurfaceView? = nil,
         moveFocusFrom oldView: Ghostty.SurfaceView? = nil,
-        undoAction: String? = nil
+        undoAction: String? = nil,
+        undoable: Bool = true
     ) {
         // We have a special case if our tree is empty to close our tab immediately.
         // This makes it so that undo is handled properly.
@@ -203,7 +204,8 @@ class TerminalController: BaseTerminalController, TabGroupCloseCoordinator.Contr
             newTree,
             moveFocusTo: newView,
             moveFocusFrom: oldView,
-            undoAction: undoAction)
+            undoAction: undoAction,
+            undoable: undoable)
     }
 
     // MARK: Terminal Creation
diff --git a/macos/Sources/Ghostty/Ghostty.App.swift b/macos/Sources/Ghostty/Ghostty.App.swift
index bf06e3e..6a56ba7 100644
--- a/macos/Sources/Ghostty/Ghostty.App.swift
+++ b/macos/Sources/Ghostty/Ghostty.App.swift
@@ -290,6 +290,29 @@ extension Ghostty {
 
         static func closeSurface(_ userdata: UnsafeMutableRawPointer?, processAlive: Bool) {
             let surface = self.surfaceUserdata(from: userdata)
+            if let session = surface.scrollbackEditorSession {
+                surface.scrollbackEditorSession = nil
+
+                if let controller = BaseTerminalController.controller(owning: surface),
+                   let node = controller.surfaceTree.root?.node(view: surface) {
+                    do {
+                        let restored = try controller.surfaceTree.replacing(
+                            node: node,
+                            with: .leaf(view: session.original)
+                        )
+                        controller.replaceSurfaceTree(
+                            restored,
+                            moveFocusTo: session.original,
+                            moveFocusFrom: surface,
+                            undoable: false
+                        )
+                        return
+                    } catch {
+                        logger.warning("failed to restore scrollback editor surface: \(error, privacy: .public)")
+                    }
+                }
+            }
+
             NotificationCenter.default.post(name: Notification.ghosttyCloseSurface, object: surface, userInfo: [
                 "process_alive": processAlive,
             ])
@@ -741,6 +764,9 @@ extension Ghostty {
             case GHOSTTY_ACTION_OPEN_URL:
                 return openURL(action.action.open_url)
 
+            case GHOSTTY_ACTION_EDIT_SCROLLBACK:
+                return editScrollback(app, target: target, value: action.action.edit_scrollback)
+
             case GHOSTTY_ACTION_UNDO:
                 return undo(app, target: target)
 
@@ -852,6 +878,81 @@ extension Ghostty {
             return true
         }
 
+        final class ScrollbackEditorSession {
+            let original: Ghostty.SurfaceView
+            let directory: URL
+
+            init(
+                original: Ghostty.SurfaceView,
+                directory: URL
+            ) {
+                self.original = original
+                self.directory = directory
+            }
+
+            deinit {
+                do {
+                    try FileManager.default.removeItem(at: directory)
+                } catch {
+                    let cocoaError = error as NSError
+                    if cocoaError.code != NSFileNoSuchFileError {
+                        Ghostty.logger.warning(
+                            "failed to delete scrollback directory: \(error, privacy: .public)"
+                        )
+                    }
+                }
+            }
+        }
+
+        private static func editScrollback(
+            _ app: ghostty_app_t,
+            target: ghostty_target_s,
+            value: ghostty_action_edit_scrollback_s
+        ) -> Bool {
+            guard target.tag == GHOSTTY_TARGET_SURFACE,
+                  let coreSurface = target.target.surface,
+                  let original = surfaceView(from: coreSurface),
+                  let controller = BaseTerminalController.controller(owning: original),
+                  let node = controller.surfaceTree.root?.node(view: original),
+                  let directoryPointer = value.directory,
+                  let commandPointer = value.command
+            else { return false }
+
+            let directoryData = Data(bytes: directoryPointer, count: Int(value.directory_len))
+            guard let directory = String(data: directoryData, encoding: .utf8) else { return false }
+
+            var config = SurfaceConfiguration(
+                from: ghostty_surface_inherited_config(coreSurface, GHOSTTY_SURFACE_CONTEXT_SPLIT)
+            )
+            config.command = String(cString: commandPointer)
+            config.waitAfterCommand = false
+            config.context = GHOSTTY_SURFACE_CONTEXT_SPLIT
+
+            let editor = SurfaceView(app, baseConfig: config)
+            guard editor.surface != nil else { return false }
+
+            do {
+                let replacement = try controller.surfaceTree.replacing(
+                    node: node,
+                    with: .leaf(view: editor)
+                )
+                editor.scrollbackEditorSession = .init(
+                    original: original,
+                    directory: URL(filePath: directory)
+                )
+                controller.replaceSurfaceTree(
+                    replacement,
+                    moveFocusTo: editor,
+                    moveFocusFrom: original,
+                    undoable: false
+                )
+                return true
+            } catch {
+                logger.warning("failed to open scrollback editor surface: \(error, privacy: .public)")
+                return false
+            }
+        }
+
         private static func openUntrustedURL(_ value: String) -> Bool {
             let target = UntrustedURL(value)
             switch target.decision {
diff --git a/macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift b/macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift
index f77985f..30b005e 100644
--- a/macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift	
+++ b/macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift	
@@ -180,6 +180,12 @@ extension Ghostty {
         /// state so we're mixing this with direct surface access.
         private(set) var surfaceModel: Ghostty.Surface?
 
+        /// Present only while this view is acting as a temporary scrollback
+        /// editor. Keeping the state on the view ties cleanup to the view's
+        /// lifecycle, including window teardown paths that do not emit a
+        /// normal surface-close callback.
+        var scrollbackEditorSession: App.ScrollbackEditorSession?
+
         /// Returns the underlying C value for the surface. See "note" on surfaceModel.
         override var surface: ghostty_surface_t? {
             surfaceModel?.unsafeCValue
diff --git a/src/Surface.zig b/src/Surface.zig
index 7752921..987db82 100644
--- a/src/Surface.zig
+++ b/src/Surface.zig
@@ -337,6 +337,7 @@ const DerivedConfig = struct {
     links: []DerivedConfig.Link,
     link_osc8: bool,
     link_previews: configpkg.LinkPreviews,
+    scrollback_editor: ?configpkg.Command,
     scroll_to_bottom: configpkg.Config.ScrollToBottom,
     notify_on_command_finish: configpkg.Config.NotifyOnCommandFinish,
     notify_on_command_finish_action: configpkg.Config.NotifyOnCommandFinishAction,
@@ -417,6 +418,10 @@ const DerivedConfig = struct {
             .links = links,
             .link_osc8 = config.@"link-osc8",
             .link_previews = config.@"link-previews",
+            .scrollback_editor = if (config.@"scrollback-editor") |editor|
+                try editor.clone(alloc)
+            else
+                null,
             .scroll_to_bottom = config.@"scroll-to-bottom",
             .notify_on_command_finish = config.@"notify-on-command-finish",
             .notify_on_command_finish_action = config.@"notify-on-command-finish-action",
@@ -5690,6 +5695,24 @@ const WriteScreenLoc = enum {
     selection, // Selected text
 };
 
+/// Build the command used by the terminal scrollback editor. Shell commands
+/// retain their configured expansion behavior. Direct commands are quoted
+/// argument-by-argument because embedded runtimes accept a shell string.
+fn scrollbackEditorCommand(
+    alloc: Allocator,
+    editor: configpkg.Command,
+    path: []const u8,
+) ![:0]const u8 {
+    var command = internal_os.shell.ShellCommandBuilder.init(alloc);
+    defer command.deinit();
+    switch (editor) {
+        .shell => |value| try command.appendArg(value),
+        .direct => |argv| for (argv) |arg| try command.appendQuotedArg(arg),
+    }
+    try command.appendQuotedArg(path);
+    return try command.toOwnedSlice();
+}
+
 fn writeScreenFile(
     self: *Surface,
     loc: WriteScreenLoc,
@@ -5740,15 +5763,14 @@ fn writeScreenFile(
         const pages = &self.io.terminal.screens.active.pages;
         const sel_: ?terminal.Selection = switch (loc) {
             .history => history: {
-                // We do not support this for alternate screens
-                // because they don't have scrollback anyways.
-                if (self.io.terminal.screens.active_key == .alternate) {
-                    break :history null;
-                }
-
                 break :history terminal.Selection.init(
-                    pages.getTopLeft(.history),
-                    pages.getBottomRight(.history) orelse
+                    pages.getTopLeft(
+                        if (self.io.terminal.screens.active_key == .alternate)
+                            .screen
+                        else
+                            .history,
+                    ),
+                    pages.getBottomRight(.screen) orelse
                         break :history null,
                     false,
                 );
@@ -5809,13 +5831,44 @@ fn writeScreenFile(
                 .data = pathZ,
             }}, false);
         },
-        .open => try self.openUrl(.{
-            .kind = switch (write_screen.emit) {
-                .plain, .vt => .text,
-                .html => .html,
-            },
-            .url = path,
-        }),
+        .open => open: {
+            // A configured scrollback editor gets the first opportunity to
+            // open plain-text history in a temporary terminal surface. App
+            // runtimes may decline this action, in which case the normal OS
+            // opener remains the fallback.
+            if (loc == .history and write_screen.emit != .html) {
+                if (self.config.scrollback_editor) |editor| {
+                    var arena: ArenaAllocator = .init(self.alloc);
+                    defer arena.deinit();
+
+                    const command = try scrollbackEditorCommand(
+                        arena.allocator(),
+                        editor,
+                        path,
+                    );
+                    const handled = self.rt_app.performAction(
+                        .{ .surface = self },
+                        .edit_scrollback,
+                        .{
+                            .directory = std.fs.path.dirname(path).?,
+                            .command = command,
+                        },
+                    ) catch |err| handled: {
+                        log.warn("unable to open terminal scrollback editor err={}", .{err});
+                        break :handled false;
+                    };
+                    if (handled) break :open;
+                }
+            }
+
+            try self.openUrl(.{
+                .kind = switch (write_screen.emit) {
+                    .plain, .vt => .text,
+                    .html => .html,
+                },
+                .url = path,
+            });
+        },
         .paste => self.queueIo(try termio.Message.writeReq(
             self.alloc,
             path,
@@ -5826,6 +5879,32 @@ fn writeScreenFile(
     retain_tmp_dir = true;
 }
 
+test "scrollback editor command uses the shared shell command builder" {
+    const testing = std.testing;
+
+    const shell = try scrollbackEditorCommand(
+        testing.allocator,
+        .{ .shell = "hx --readonly" },
+        "/tmp/it's scrollback.txt",
+    );
+    defer testing.allocator.free(shell);
+    try testing.expectEqualStrings(
+        "hx --readonly '/tmp/it'\"'\"'s scrollback.txt'",
+        shell,
+    );
+
+    const direct = try scrollbackEditorCommand(
+        testing.allocator,
+        .{ .direct = &.{ "terminal theme", "hx" } },
+        "/tmp/scrollback.txt",
+    );
+    defer testing.allocator.free(direct);
+    try testing.expectEqualStrings(
+        "'terminal theme' hx /tmp/scrollback.txt",
+        direct,
+    );
+}
+
 /// The payload for completing a clipboard request with
 /// completeClipboardRequest.
 pub const CompleteClipboard = struct {
diff --git a/src/apprt/action.zig b/src/apprt/action.zig
index 7e902e5..f4ddff3 100644
--- a/src/apprt/action.zig
+++ b/src/apprt/action.zig
@@ -357,6 +357,11 @@ pub const Action = union(Key) {
     /// Move a tab to a new window.
     move_tab_to_new_window,
 
+    /// Open an exported scrollback file in a temporary terminal surface.
+    /// The application runtime should restore the original target surface
+    /// when the command exits and remove the temporary file directory.
+    edit_scrollback: EditScrollback,
+
     /// Sync with: ghostty_action_tag_e
     pub const Key = enum(c_int) {
         quit,
@@ -428,6 +433,7 @@ pub const Action = union(Key) {
         readonly,
         copy_title_to_clipboard,
         move_tab_to_new_window,
+        edit_scrollback,
 
         test "ghostty.h Action.Key" {
             try lib.checkGhosttyHEnum(Key, "GHOSTTY_ACTION_");
@@ -502,6 +508,32 @@ pub const Action = union(Key) {
     }
 };
 
+/// Open an exported scrollback file in a terminal-based editor.
+pub const EditScrollback = struct {
+    /// The directory containing the exported scrollback. The application
+    /// runtime owns its cleanup after accepting this action.
+    directory: []const u8,
+
+    /// A shell command containing the configured editor and the safely escaped
+    /// scrollback path. This is sentinel-terminated for surface creation APIs.
+    command: [:0]const u8,
+
+    // Sync with: ghostty_action_edit_scrollback_s
+    pub const C = extern struct {
+        directory: [*]const u8,
+        directory_len: usize,
+        command: [*:0]const u8,
+    };
+
+    pub fn cval(self: EditScrollback) C {
+        return .{
+            .directory = self.directory.ptr,
+            .directory_len = self.directory.len,
+            .command = self.command.ptr,
+        };
+    }
+};
+
 // This is made extern (c_int) to make interop easier with our embedded
 // runtime. The small size cost doesn't make a difference in our union.
 pub const SplitDirection = enum(c_int) {
diff --git a/src/apprt/gtk/class/application.zig b/src/apprt/gtk/class/application.zig
index 5b9d3bf..4f1dee4 100644
--- a/src/apprt/gtk/class/application.zig
+++ b/src/apprt/gtk/class/application.zig
@@ -743,6 +743,12 @@ pub const Application = extern struct {
 
             .open_url => Action.openUrl(self, value),
 
+            .edit_scrollback => return Action.editScrollback(
+                self,
+                target,
+                value,
+            ),
+
             .pwd => Action.pwd(target, value),
 
             .present_terminal => return Action.presentTerminal(target),
@@ -2849,6 +2855,149 @@ const Action = struct {
         };
     }
 
+    pub fn editScrollback(
+        self: *Application,
+        target: apprt.Target,
+        value: apprt.action.EditScrollback,
+    ) bool {
+        const core = switch (target) {
+            .app => return false,
+            .surface => |surface| surface,
+        };
+        const original = core.rt_surface.surface;
+        const split_tree = ext.getAncestor(
+            SplitTree,
+            original.as(gtk.Widget),
+        ) orelse return false;
+
+        // Only replace a surface that still occupies its pane. This also
+        // avoids racing a second transient replacement.
+        if (split_tree.getActiveSurface() != original) return false;
+
+        const editor: *Surface = .new(.{
+            .command = .{ .shell = value.command },
+            .wait_after_command = false,
+            .title = std.mem.span(i18n._("Scrollback")),
+        });
+        _ = editor.refSink();
+        editor.setParent(core, .split);
+        editor.bindIsSplit(split_tree);
+
+        const alloc = self.allocator();
+        const temp_dir = alloc.dupe(u8, value.directory) catch {
+            editor.unref();
+            return false;
+        };
+        const state = alloc.create(EditScrollbackState) catch {
+            alloc.free(temp_dir);
+            editor.unref();
+            return false;
+        };
+        state.* = .{
+            .app = self,
+            .original = original.ref(),
+            .temp_dir = temp_dir,
+        };
+        state.split_tree.set(split_tree);
+
+        state.close_handler = Surface.signals.@"close-request".connect(
+            editor,
+            *EditScrollbackState,
+            editScrollbackClose,
+            state,
+            .{},
+        );
+        editor.as(gobject.Object).weakRef(editScrollbackWeakNotify, state);
+
+        if (!(split_tree.replaceSurface(original, editor) catch false)) {
+            editor.as(gobject.Object).weakUnref(editScrollbackWeakNotify, state);
+            gobject.signalHandlerDisconnect(
+                editor.as(gobject.Object),
+                state.close_handler,
+            );
+            state.split_tree.deinit();
+            state.original.unref();
+            alloc.free(state.temp_dir);
+            alloc.destroy(state);
+            editor.unref();
+            return false;
+        }
+
+        // The split tree now owns the editor. The state deliberately keeps
+        // only a weak association so closing the containing window cannot
+        // form a reference cycle.
+        editor.unref();
+        editor.grabFocus();
+        return true;
+    }
+
+    const EditScrollbackState = struct {
+        app: *Application,
+        split_tree: WeakRef(SplitTree) = .empty,
+        original: *Surface,
+        temp_dir: []u8,
+        close_handler: c_ulong = 0,
+        restoring: bool = false,
+    };
+
+    fn editScrollbackClose(
+        editor: *Surface,
+        state: *EditScrollbackState,
+    ) callconv(.c) void {
+        if (state.restoring) return;
+        state.restoring = true;
+
+        editor.as(gobject.Object).weakUnref(editScrollbackWeakNotify, state);
+        gobject.signalHandlerDisconnect(
+            editor.as(gobject.Object),
+            state.close_handler,
+        );
+        state.close_handler = 0;
+
+        if (state.split_tree.get()) |split_tree| {
+            defer split_tree.unref();
+            if (split_tree.replaceSurface(editor, state.original) catch false) {
+                state.original.grabFocus();
+            }
+        }
+
+        // Release the original surface and delete the export only after the
+        // close signal has finished emitting.
+        _ = glib.idleAdd(editScrollbackCleanup, state);
+    }
+
+    fn editScrollbackWeakNotify(
+        ud: ?*anyopaque,
+        _: *gobject.Object,
+    ) callconv(.c) void {
+        const state: *EditScrollbackState = @ptrCast(@alignCast(
+            ud orelse return,
+        ));
+        destroyEditScrollbackState(state);
+    }
+
+    fn editScrollbackCleanup(ud: ?*anyopaque) callconv(.c) c_int {
+        const state: *EditScrollbackState = @ptrCast(@alignCast(
+            ud orelse return 0,
+        ));
+        destroyEditScrollbackState(state);
+        return 0;
+    }
+
+    fn destroyEditScrollbackState(state: *EditScrollbackState) void {
+        state.split_tree.deinit();
+        state.original.unref();
+
+        const app = state.app;
+        std.Io.Dir.cwd().deleteTree(global.io(), state.temp_dir) catch |err|
+            log.warn("failed to delete scrollback directory path={s} err={}", .{
+                state.temp_dir,
+                err,
+            });
+        app.allocator().free(state.temp_dir);
+        app.allocator().destroy(state);
+    }
+
     pub fn pwd(
         target: apprt.Target,
         value: apprt.action.Pwd,
diff --git a/src/apprt/gtk/class/split_tree.zig b/src/apprt/gtk/class/split_tree.zig
index ca74371..5110a72 100644
--- a/src/apprt/gtk/class/split_tree.zig
+++ b/src/apprt/gtk/class/split_tree.zig
@@ -455,6 +455,27 @@ pub const SplitTree = extern struct {
         }
     }
 
+    /// Replace a surface without changing its position in the split tree.
+    pub fn replaceSurface(
+        self: *Self,
+        old: *Surface,
+        new: *Surface,
+    ) Allocator.Error!bool {
+        const old_tree = self.getTree() orelse return false;
+        const handle = old_tree.locate(old) orelse return false;
+
+        var new_tree = try old_tree.replace(
+            Application.default().allocator(),
+            handle,
+            new,
+        );
+        defer new_tree.deinit();
+
+        self.private().last_focused.set(new);
+        self.setTree(&new_tree);
+        return true;
+    }
+
     fn disconnectSurfaceHandlers(self: *Self) void {
         const tree = self.getTree() orelse return;
         var it = tree.iterator();
diff --git a/src/config/Config.zig b/src/config/Config.zig
index 877c437..efb87f3 100644
--- a/src/config/Config.zig
+++ b/src/config/Config.zig
@@ -1437,6 +1437,24 @@ input: RepeatableReadableIO = .{},
 /// already compressed remain compressed until their contents are accessed.
 @"scrollback-compression": bool = true,
 
+/// A terminal-based editor to use when opening a plain-text scrollback file.
+/// When this is set, `write_scrollback_file:open` temporarily replaces the
+/// current terminal with the configured command and restores it when the
+/// editor exits. The temporary file path is appended as the final argument.
+///
+/// If the application runtime cannot host the editor in a terminal surface,
+/// Ghostty falls back to the operating system's default text editor.
+///
+/// For example:
+///
+/// ```ini
+/// scrollback-editor = direct:hx
+/// ```
+///
+/// Available since: 1.4.0.
+///
+@"scrollback-editor": ?Command = null,
+
 /// Control when the scrollbar is shown to scroll the scrollback buffer.
 ///
 /// The default value is `system`.
@@ -11165,6 +11183,26 @@ test "clipboard write limit" {
     );
 }
 
+test "scrollback editor command" {
+    const testing = std.testing;
+    const alloc = testing.allocator;
+
+    var cfg = try Config.default(alloc);
+    defer cfg.deinit();
+    try testing.expectEqual(null, cfg.@"scrollback-editor");
+
+    var it: TestIterator = .{ .data = &.{
+        "--scrollback-editor=direct:terminal-theme-run hx",
+    } };
+    try cfg.loadIter(alloc, &it);
+
+    const editor = cfg.@"scrollback-editor".?;
+    try testing.expect(editor == .direct);
+    try testing.expectEqual(@as(usize, 2), editor.direct.len);
+    try testing.expectEqualStrings("terminal-theme-run", editor.direct[0]);
+    try testing.expectEqualStrings("hx", editor.direct[1]);
+}
+
 test "compatibility: scrollback-limit renamed to bytes" {
     const testing = std.testing;
     const alloc = testing.allocator;
