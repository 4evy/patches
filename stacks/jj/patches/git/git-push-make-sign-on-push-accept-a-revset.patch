From bf11d74556601a5606fe67bc80cb93aa29a3d2e1 Mon Sep 17 00:00:00 2001
From: 4evy <git@evy.pink>
Date: Thu, 1 Jan 1970 00:00:00 +0000
Subject: git push: make sign-on-push accept a revset

Let users define signing policy with the same revset language used by other jj configuration. Intersect the configured selector with commits outgoing to the pushed references and force-sign exactly the selected commits.

Default to none() and migrate boolean values for six releases so existing configurations keep their old mutable, unsigned, own-commit behavior.

diff --git a/CHANGELOG.md b/CHANGELOG.md
index 7773be29919b0e33b1d02d76a2c422556168a253..d51fc1f3bde5a97820b472598bcd51dc3bb553f6 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -35,6 +35,10 @@ to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
 * `jj git import` in non-colocated repositories no longer imports commits from a
   detached Git HEAD branch.
 
+* `git.sign-on-push` is now a revset selecting commits to sign instead of a
+  boolean. The default is `none()`. Existing `true` values are migrated to
+  `~signed() & mine()` and `false` values to `none()`.
+
 ### Deprecations
 
 None
diff --git a/cli/src/commands/git/push.rs b/cli/src/commands/git/push.rs
index 860ef6ccdb7d70935fff4d3a801675f25d5e6338..f552a1ccbd56cd1454deacfe145766d4293cecbf 100644
--- a/cli/src/commands/git/push.rs
+++ b/cli/src/commands/git/push.rs
@@ -14,7 +14,6 @@
 
 use std::collections::HashMap;
 use std::collections::HashSet;
-use std::future;
 use std::io;
 use std::io::Write as _;
 use std::iter;
@@ -535,9 +534,14 @@ pub async fn cmd_git_push(
         return Ok(());
     }
 
-    if !args.dry_run && tx.settings().get_bool("git.sign-on-push")? {
-        let to_push_expr = ready_to_push_revset_expression(&tx, remote, &ref_updates);
-        ref_updates = sign_commits_before_push(ui, &mut tx, to_push_expr, ref_updates).await?;
+    let sign_on_push_revset = tx.settings().get_string("git.sign-on-push")?;
+    let mut commits_to_sign = tx
+        .base_workspace_helper()
+        .parse_revset(ui, &RevisionArg::from(sign_on_push_revset))?;
+    commits_to_sign.intersect_with(&ready_to_push_revset_expression(&tx, remote, &ref_updates));
+    let commits_to_sign = commits_to_sign.resolve()?;
+    if !args.dry_run {
+        ref_updates = sign_commits_before_push(ui, &mut tx, commits_to_sign, ref_updates).await?;
     }
 
     if let Some(mut formatter) = ui.status_formatter() {
@@ -811,22 +815,18 @@ fn ready_to_push_revset_expression(
 async fn sign_commits_before_push(
     ui: &Ui,
     tx: &mut WorkspaceCommandTransaction<'_>,
-    commits_to_push: Arc<UserRevsetExpression>,
+    commits_to_sign: Arc<ResolvedRevsetExpression>,
     ref_updates: GitPushRefTargets,
 ) -> Result<GitPushRefTargets, CommandError> {
-    let mut sign_settings = tx.settings().sign_settings();
-    sign_settings.behavior = SignBehavior::Own;
-    // TODO: make filter condition configurable by revset?
-    let needs_signing = |commit: &Commit| {
-        future::ready(!commit.is_signed() && sign_settings.should_sign(commit.store_commit()))
-    };
-
     let workspace_helper = tx.base_workspace_helper();
-    let immutable = workspace_helper.env().immutable_expression();
-    let skipped: Vec<Commit> = workspace_helper
-        .attach_revset_evaluator(commits_to_push.intersection(&immutable))
-        .evaluate_to_commits()?
-        .try_filter(needs_signing)
+    let immutable = workspace_helper
+        .attach_revset_evaluator(workspace_helper.env().immutable_expression())
+        .resolve()?;
+    let skipped: Vec<Commit> = commits_to_sign
+        .intersection(&immutable)
+        .evaluate(tx.base_repo().as_ref())?
+        .stream()
+        .commits(tx.base_repo().store())
         .take(11)
         .try_collect()
         .await?;
@@ -848,10 +848,11 @@ async fn sign_commits_before_push(
         )?;
     }
 
-    let commit_ids: IndexSet<CommitId> = workspace_helper
-        .attach_revset_evaluator(commits_to_push.minus(&immutable))
-        .evaluate_to_commits()?
-        .try_filter(needs_signing)
+    let commit_ids: IndexSet<CommitId> = commits_to_sign
+        .minus(&immutable)
+        .evaluate(tx.base_repo().as_ref())?
+        .stream()
+        .commits(tx.base_repo().store())
         .map_ok(|commit| commit.id().clone())
         .try_collect()
         .await?;
@@ -878,7 +879,7 @@ async fn sign_commits_before_push(
                     if commit_ids.contains(&old_commit_id) {
                         let commit = rewriter
                             .reparent()
-                            .set_sign_behavior(sign_settings.behavior)
+                            .set_sign_behavior(SignBehavior::Force)
                             .write()
                             .await?;
                         old_to_new_commits_map.insert(old_commit_id, commit.id().clone());
diff --git a/cli/src/config-schema.json b/cli/src/config-schema.json
index a19b338da7d97daed42672966d11617fd83bd096..3f2d1f30cc0f1b763d5c26cadabf4cf449567cfe 100644
--- a/cli/src/config-schema.json
+++ b/cli/src/config-schema.json
@@ -536,9 +536,9 @@
                     "default": true
                 },
                 "sign-on-push": {
-                    "type": "boolean",
-                    "description": "Whether jj should sign commits before pushing",
-                    "default": false
+                    "type": "string",
+                    "description": "Revset of commits to sign before pushing",
+                    "default": "none()"
                 },
                 "track-default-bookmark-on-clone": {
                     "type": "boolean",
diff --git a/cli/src/config.rs b/cli/src/config.rs
index 6459729a0df20ce88c67a7e39bd539a0eb9f4be0..b38ffe8c2827d1cbf8f814fb1d1dd3dd65e747b9 100644
--- a/cli/src/config.rs
+++ b/cli/src/config.rs
@@ -963,7 +963,32 @@ fn parse_config_arg_item(item_str: &str) -> Result<(ConfigNamePathBuf, ConfigVal
 
 /// List of rules to migrate deprecated config variables.
 pub fn default_config_migrations() -> Vec<ConfigMigrationRule> {
-    vec![]
+    vec![
+        // TODO: Delete in jj 0.50.0+
+        ConfigMigrationRule::custom(
+            |layer| {
+                layer
+                    .look_up_item("git.sign-on-push")
+                    .is_ok_and(|value| value.is_some_and(|value| value.as_bool().is_some()))
+            },
+            |layer| match layer.look_up_item("git.sign-on-push") {
+                Ok(Some(value)) => {
+                    let old_value = value.as_bool().unwrap();
+                    let new_value = if old_value {
+                        "~signed() & mine()"
+                    } else {
+                        "none()"
+                    };
+                    layer.set_value("git.sign-on-push", new_value.to_owned())?;
+                    Ok(format!(
+                        "git.sign-on-push = {old_value} is updated to git.sign-on-push = \
+                         '{new_value}'"
+                    ))
+                }
+                _ => unreachable!(),
+            },
+        ),
+    ]
 }
 
 /// Command name and arguments specified by config.
diff --git a/cli/src/config/misc.toml b/cli/src/config/misc.toml
index 70ed210c17e54ed3f39f040a48bafa799565dc29..0178cf95532230260042eae2fb1f04f5bdc25069 100644
--- a/cli/src/config/misc.toml
+++ b/cli/src/config/misc.toml
@@ -28,7 +28,7 @@ disabled-branches = []
 colocate = true
 object-hash = "sha1"
 private-commits = "none()"
-sign-on-push = false
+sign-on-push = "none()"
 track-default-bookmark-on-clone = true
 
 [ui]
diff --git a/cli/tests/sample-configs/valid/git.toml b/cli/tests/sample-configs/valid/git.toml
index f42fee952ec0a99039368561707bf9931fe4b79a..c0d64f8e1ce2443aed29a6ddc79bb3d3870e9a05 100644
--- a/cli/tests/sample-configs/valid/git.toml
+++ b/cli/tests/sample-configs/valid/git.toml
@@ -5,7 +5,7 @@ fetch = ["origin", "fork"]
 fetch-depth = 42
 push = "fork"
 record-synthetic-predecessors = false
-sign-on-push = true
+sign-on-push = "mine()"
 subprocess = false
 write-change-id-header = true
 executable-path = "/home/linus/.local/bin/git"
diff --git a/cli/tests/test_config_command.rs b/cli/tests/test_config_command.rs
index 61a489a1113cb641d373bae679cf66320e26546b..9abd2913769e4dcd0de27222111e1074a23f5db1 100644
--- a/cli/tests/test_config_command.rs
+++ b/cli/tests/test_config_command.rs
@@ -27,6 +27,45 @@ use crate::common::fake_editor_path;
 use crate::common::force_interactive;
 use crate::common::to_toml_value;
 
+#[test]
+fn test_config_migrate_sign_on_push_boolean() {
+    let test_env = TestEnvironment::default();
+
+    let output = test_env.run_jj_in(
+        ".",
+        [
+            "config",
+            "list",
+            "git.sign-on-push",
+            "--config=git.sign-on-push=true",
+        ],
+    );
+    insta::assert_snapshot!(output, @r#"
+    git.sign-on-push = "~signed() & mine()"
+    [EOF]
+    ------- stderr -------
+    Warning: Deprecated CLI-provided config: git.sign-on-push = true is updated to git.sign-on-push = '~signed() & mine()'
+    [EOF]
+    "#);
+
+    let output = test_env.run_jj_in(
+        ".",
+        [
+            "config",
+            "list",
+            "git.sign-on-push",
+            "--config=git.sign-on-push=false",
+        ],
+    );
+    insta::assert_snapshot!(output, @r#"
+    git.sign-on-push = "none()"
+    [EOF]
+    ------- stderr -------
+    Warning: Deprecated CLI-provided config: git.sign-on-push = false is updated to git.sign-on-push = 'none()'
+    [EOF]
+    "#);
+}
+
 #[test]
 fn test_config_list_single() {
     let test_env = TestEnvironment::default();
diff --git a/cli/tests/test_git_push.rs b/cli/tests/test_git_push.rs
index 23b83e59334fb1ca7505f4373faf4c02ee0939e4..8b3914a9c66d294dd83d783c296638628616fb66 100644
--- a/cli/tests/test_git_push.rs
+++ b/cli/tests/test_git_push.rs
@@ -3017,7 +3017,7 @@ fn test_git_push_sign_on_push() {
         r#"
     signing.backend = "test"
     signing.key = "impeccable"
-    git.sign-on-push = true
+    git.sign-on-push = "~signed() & mine()"
     "#,
     );
     let output = work_dir.run_jj(["git", "push", "--dry-run"]);
diff --git a/docs/config.md b/docs/config.md
index 5d4bb11af28b9d33466541aaabc411134a853e8a..076bf44df428f387215362bb0da24f7f2d94494d 100644
--- a/docs/config.md
+++ b/docs/config.md
@@ -1730,22 +1730,21 @@ to do with signing commits on modification of a change (e.g., rebasing or edits)
 - `force`: sign all commits after modification, always, even if you are not the
   author.
 
-Instead of signing all commits during creation when `signing.behavior` is
-set to `own`, the `git.sign-on-push` configuration can be used to sign
-commits only upon running `jj git push`. All mutable unsigned commits
-being pushed will be signed prior to pushing. This might be preferred if the
-signing backend requires user interaction or is slow, so that signing is
-performed in a single batch operation.
+If the signing backend requires user interaction for every signature, or is
+slow, you might not want to sign each commit at creation time. Instead, the
+`git.sign-on-push` configuration can be set to a revset of commits to sign only
+upon running `jj git push`. Matching mutable commits being pushed will be
+signed prior to pushing.
 
 ```toml
 # Configure signing backend as before, but lazily signing only on push.
 [signing]
-behavior = "drop"
+behavior = "drop" # Avoid signing commits when they are created
 backend = "ssh"
 key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIGj+J6N6SO+4P8dOZqfR1oiay2yxhhHnagH52avUqw5h"
 
 [git]
-sign-on-push = true
+sign-on-push = "~signed() & mine()"
 ```
 
 ## Commit Signature Verification
